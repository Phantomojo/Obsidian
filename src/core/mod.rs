let identity = Arc::new(Identity::new()?);
// TODO: Implement get_topology() or expose topology field 
 
// In get_mesh_topology, add a TODO or use a public method if available:
// TODO: Implement get_topology() or expose topology field 